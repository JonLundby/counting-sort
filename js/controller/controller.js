"use strict";

import * as model from "../model/model.js";
import * as view from "../view/view.js";

window.addEventListener("load", initApp);

function initApp() {
    console.log(model.unsortedArr);
    console.log(model.sortedArr);
}
